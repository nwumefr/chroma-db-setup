# Setup
setup an empty directory called tmp for the chroma db collection 

also install the requirements 

for google stuff you need to setup an api key for gemini here->https://aistudio.google.com/

the notes file is specifically stuff i wrote while coding in the recording. you can ignore that unless you really want to read it.


# Web app setup
To run the web app, get a Gemini API key and put it into an .env file in the app/ directory, then cd into app/ and run 
```bash
npm run setup
```
```bash
npm run dev
```
to launch both the front-end and back-end, and access the front-end at localhost:5173
