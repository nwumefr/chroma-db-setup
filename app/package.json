{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-api": "cd api && python main.py --reload",
    "start-ui": "cd ui && npm run dev",
    "dev": "concurrently \"npm run start-api\" \"npm run start-ui\"",
    "setup": "npm install && cd api && pip install -r requirements.txt && cd ../ui && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
